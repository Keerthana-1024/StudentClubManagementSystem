<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile</title>
    <link href="https://fonts.googleapis.com/css2?family=Open+Sans:wght@400;600&display=swap" rel="stylesheet">
    <style>
        /* Reset box-sizing and set base font */
        * {
            box-sizing: border-box;
        }

        html {
            font-size: 16px;
        }

        body {
            margin: 0;
            font-family: 'Open Sans', sans-serif;
            background: rgb(60, 6, 88);
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }

        .profile-container {
            background-color: rgba(255, 255, 255, 0.9);
            border-radius: 20px;
            box-shadow: 0 20px 50px rgba(0, 0, 0, 0.3);
            padding: 10px;
            padding-left: 25px;
            padding-right: 25px;
            max-width: 800px;
            width: 100%;
            overflow: hidden;
            position: relative;
        }

        .card {
            background-color: #fff;
            border-radius: 15px;
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
            padding: 30px;
            margin-bottom: 30px;
        }

        h1 {
            text-align: center;
            text-transform: uppercase;
            margin-bottom: 50px;
            color: #333;
            font-size: 3rem;
            letter-spacing: 3px;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3);
        }

        .card h2 {
            margin-bottom: 20px;
            color: #333;
            font-size: 2rem;
        }

        .card p {
            margin-bottom: 15px;
            color: #555;
            font-size: 1.3rem;
        }

        ul {
            list-style: none;
            padding: 0;
            margin-top: 15px;
        }

        ul li {
            margin-bottom: 10px;
            color: #777;
            font-size: 1.2rem;
            transition: color 0.3s ease;
        }

        ul li:hover {
            color: #333;
            cursor: pointer;
        }

        .fancy-decoration {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
        }

        .fancy-decoration:before,
        .fancy-decoration:after {
            content: '';
            position: absolute;
            top: 0;
            width: 50%;
            height: 100%;
            background: radial-gradient(circle, #3498db 20%, transparent 70%);
        }

        .fancy-decoration:before {
            left: 0;
            transform: skewX(-30deg);
        }

        .fancy-decoration:after {
            right: 0;
            transform: skewX(30deg);
        }
    </style>
</head>
<body>
    <div class="profile-container">
        <h1>User Profile</h1>
        <div class="card">
            <h2>Personal Information</h2>
            <p><strong>Name:</strong> <%= name %></p>
            <p><strong>Email:</strong> <%= email %></p>
        </div>

        <div class="card">
            <% if (events.length > 0) { %>
                <h2>Events</h2>
                <ul>
                    <% events.forEach(event => { %>
                        <li><%= event.event_name %></li>
                    <% }); %>
                </ul>
            <% } else { %>
                <p>No events</p>
            <% } %>
        </div>

        <div class="card">
            <% if (clubs.length > 0) { %>
                <h2>Club Memberships</h2>
                <ul>
                    <% clubs.forEach(club => { %>
                        <li>
                            <%= club.club_id %>
                            <%= club.position %>
                            <% if (club.subsystem_id !== null) { %>
                                <%= club.subsystem_id %>
                            <% } %>
                        </li>
                    <% }); %>
                </ul>
            <% } else { %>
                <p>No club memberships</p>
            <% } %>
        </div>

        <div class="fancy-decoration"></div>
    </div>
</body>
</html>


