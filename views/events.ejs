<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Events Page</title>
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  <style>
    .grid-container {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
            gap: 20px;
            padding: 20px;
        }
        .grid-item {
            background-color: #c2c5c9;
            color: black;
            padding: 20px;
            border-radius: 8px;
            text-align: center;
        }
        .green{
            background-color: rgb(43, 142, 43);
        }
        .red{
            background-color: rgb(132, 47, 47);
        }
  </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand" href="#">Login</a> 
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ml-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="https://www.geeksforgeeks.org/how-to-select-all-child-elements-recursively-using-css/">About</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="https://getbootstrap.com/">Gallery</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#h1">Events</a> 
                    </li>
                </ul>
            </div>
        </div>
    </nav>
    <!-- <div class="left">
        <ul><li>one</li><li>two</li></ul>
    </div> -->
    <div class="container">
        <h1 class="mt-5">Events</h1>
        <div class="grid-container">
            <% records.forEach(record=>{ %>
                <div class="grid-item">
                    <h5><%= record.event_name %></h5>
                    <p><%= record.start_time%> - <%= record.end_time %></p>
                    <p><%= record.venue%></p>
                    <% if(record.certifiability=='Y'){ %>
                        <div class="green">
                        <p>Certifiable</p></div>
                    <% }    else { %>
                        <div class="red">
                        <p> Not Certifiable</p></div>
                    <%}%>
                </div>
            <% })%>
        </div>
    </div>
    

    <!-- Include jQuery and Bootstrap JS for Bootstrap 4 -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.3/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>

